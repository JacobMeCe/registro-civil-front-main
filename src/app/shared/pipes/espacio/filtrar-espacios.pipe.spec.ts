import { FiltrarEspaciosPipe } from './filtrar-espacios.pipe';

describe('FiltrarEspaciosPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrarEspaciosPipe();
    expect(pipe).toBeTruthy();
  });
});
